host: localhost:8000
info:
  contact:
    name: API Support
  description: sso service.
  license:
    name: Apache 2.0
  title: Swagger API
  version: "1.0"
paths: {}
securityDefinitions:
  BearerAuth:
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
